spring:
  config:
    import: optional:application-secret.yml

  jpa:
    hibernate:
#          ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        default_batch_fetch_size: 100
        format_sql: true
